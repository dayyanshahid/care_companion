"""Shared helpers that are not tied to one app.

`messages` holds every user-facing string the backend can return or raise,
so wording is changed in one place rather than hunted for across views and
services.
"""
